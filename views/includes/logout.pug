// Logout Modal
#logout.modal.fade(tabindex='-1', role='dialog', aria-labelledby='exampleModalLabel', aria-hidden='true')
  .modal-dialog(role='document')
    .modal-content(style='color: black;')
      .modal-header
        h5#exampleModalLabel.modal-title Logout
        button.close(type='button', data-dismiss='modal', aria-label='Close')
          span(aria-hidden='true') ×
      .modal-body
        | Are you sure to log out from this system?
      .modal-footer
        button.btn.btn-secondary(type='button', data-dismiss='modal') Cancel
        a.btn.color-ink(href='/logout') Logout
